/* eslint-disable no-unused-vars */

/** This is a description of the foo function. */
function foo() {
  console.log('======== just for JSDoc');
}

/**
 * Represents a book.
 * @constructor
 * @param {string} title - The title of book.
 * @param {string} author - The author of book.
 */
function Book(title, author) {
  this.title = title;
  this.author = author;
}
