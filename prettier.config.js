module.exports = {
  trailingComma: 'none', // 去掉尾部的逗号，默认值
  tabWidth: 2, // 一个tab的占据的空格数，默认值
  semi: true, // 语句后面要有 冒号
  singleQuote: true, // 使用单引号取代双引号，jsx 里的 attribute 由 jsx-single-quote 指定
  bracketSpacing: true // 对象字面量里大括号前后要有空格
};
